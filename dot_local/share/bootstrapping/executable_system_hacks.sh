#!/bin/bash

# set repeat delay
gsettings set org.gnome.desktop.peripherals.keyboard delay "uint32 217"

# set key repeat speed
gsettings set org.gnome.desktop.peripherals.keyboard repeat-interval "uint32 11"
